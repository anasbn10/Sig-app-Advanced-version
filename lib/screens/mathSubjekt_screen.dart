import 'package:flutter/material.dart';

class MathSubjects extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Math Subjects'),),);
  }
}