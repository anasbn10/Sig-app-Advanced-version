import 'package:flutter/material.dart';

class DeutschExams extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Deutsch Exams'),),);
  }
}