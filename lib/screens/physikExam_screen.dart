import 'package:flutter/material.dart';

class PhysikExams extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Physik Exams'),),);
  }
}