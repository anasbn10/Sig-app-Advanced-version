import 'package:flutter/material.dart';

class DeutschSubjects extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Deutsch Subjects'),),);
  }
}